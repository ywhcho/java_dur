# 애플리케이션 테스트 가이드

## 실행 전 준비사항

1. **JDK 11 이상 설치 확인**
   ```bash
   java -version
   ```

2. **MySQL 서버 실행 확인**
   ```bash
   # MySQL 서비스 상태 확인
   # Windows: 서비스 관리자에서 MySQL 확인
   # macOS/Linux:
   sudo systemctl status mysql
   # 또는
   brew services list | grep mysql
   ```

3. **데이터베이스 생성**
   - `DATABASE_SETUP.md` 파일의 지침을 따라 데이터베이스를 생성하세요.

4. **데이터베이스 연결 정보 설정**
   - `src/main/resources/application.properties` 파일에서 MySQL 비밀번호를 설정하세요.

## 애플리케이션 실행

### 방법 1: Maven을 사용한 실행
```bash
mvn spring-boot:run
```

### 방법 2: JAR 파일로 실행
```bash
# 먼저 빌드
mvn clean package

# 실행
java -jar target/java-dur-1.0.0.war
```

## 애플리케이션 접속

브라우저에서 다음 URL로 접속:
```
http://localhost:8080
```

## 기능 테스트 시나리오

### 1. 회원가입 및 로그인
1. 메인 페이지에서 "회원가입" 버튼 클릭
2. 필수 정보 입력:
   - 사용자명: testuser
   - 비밀번호: test123
   - 이메일: test@example.com
   - 이름: 테스트 사용자
   - 전화번호: 010-1234-5678
3. "회원가입" 버튼 클릭
4. 로그인 페이지로 자동 이동
5. 생성한 계정으로 로그인

### 2. 게시판 사용
1. 상단 메뉴에서 "게시판" 클릭
2. "글쓰기" 버튼 클릭
3. 게시글 작성:
   - 제목: 테스트 게시글
   - 내용: 이것은 테스트 내용입니다.
4. "작성" 버튼 클릭
5. 게시글 목록에서 작성한 글 확인
6. 게시글 클릭하여 상세 내용 확인
7. 조회수 증가 확인
8. "수정" 버튼으로 게시글 수정
9. "삭제" 버튼으로 게시글 삭제

### 3. 의약정보 검색
1. 상단 메뉴에서 "의약정보" 클릭
2. "성분명 검색" 선택
3. 검색어 입력: "아세트아미노펜"
4. 검색 결과 확인 (타이레놀, 게보린 등)
5. 의약품 카드 클릭하여 상세 정보 확인
6. 뒤로 가기
7. "효능별 보기" 선택
8. 효능 선택: "진통제"
9. 해당 효능의 의약품 목록 확인

### 4. 프로필 수정
1. 상단 메뉴에서 "프로필" 버튼 클릭
2. 회원정보 수정:
   - 이메일 변경
   - 이름 수정
   - 전화번호 업데이트
3. "수정하기" 버튼 클릭
4. 성공 메시지 확인

### 5. About Us 페이지
1. 상단 메뉴에서 "About Us" 클릭
2. 회사 소개 내용 확인
3. 서비스 안내 확인

### 6. 로그아웃
1. 상단 메뉴에서 "로그아웃" 버튼 클릭
2. 메인 페이지로 이동 확인
3. 로그인이 필요한 메뉴 확인 (로그인 페이지로 리다이렉트)

## 예상 결과

### 성공 시
- 모든 페이지가 정상적으로 로드
- 데이터베이스에 정보가 저장
- 페이지 간 이동이 원활
- 세션이 유지되어 로그인 상태 유지

### 오류 발생 시
1. **데이터베이스 연결 오류**
   - MySQL 서비스 실행 확인
   - `application.properties`의 연결 정보 확인
   - 데이터베이스 생성 여부 확인

2. **포트 충돌**
   - 8080 포트가 이미 사용 중인 경우
   - `application.properties`에서 포트 변경:
     ```properties
     server.port=8081
     ```

3. **JSP 페이지 오류**
   - Maven 종속성이 제대로 설치되었는지 확인
   - `mvn clean install`로 재빌드

## 로그 확인

애플리케이션 실행 중 콘솔에서 다음을 확인할 수 있습니다:
- SQL 쿼리 실행 로그
- HTTP 요청/응답 로그
- 에러 메시지

## 초기 데이터

애플리케이션 첫 실행 시 자동으로 생성되는 샘플 의약품 데이터:
- 타이레놀 (진통제)
- 부루펜 (진통제)
- 베아제 (소화제)
- 게보린 (해열제)
- 판피린 (감기약)
- 지르텍 (알레르기약)
- 아모잘탄 (고혈압약)
- 다이아벡스 (당뇨약)

## 문제 해결

### 빌드 오류
```bash
# Maven 클린 및 재빌드
mvn clean install -U
```

### 캐시 문제
```bash
# 브라우저 캐시 삭제 또는 시크릿 모드 사용
```

### 데이터베이스 초기화
```sql
-- MySQL에서 데이터베이스 삭제 후 재생성
DROP DATABASE javadur;
CREATE DATABASE javadur CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## 성능 확인

- 페이지 로드 시간
- 데이터베이스 쿼리 성능
- 메모리 사용량

## 보안 테스트

현재 구현은 개발 버전으로, 프로덕션 배포 전 다음 사항을 개선해야 합니다:
- 비밀번호 암호화 (BCrypt 등)
- CSRF 토큰 적용
- SQL Injection 방지
- XSS 방어
